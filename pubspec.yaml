name: cashwise
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0' # <-- Ini udah bener

dependencies:
  flutter:
    sdk: flutter

  # --- CORE UTAMA (WAJIB ADA) ---
  cupertino_icons: ^1.0.2
  intl: ^0.19.0 

  # --- STATE MANAGEMENT & ARSITEKTUR ---
  flutter_bloc: ^8.1.6 # Versi baru
  equatable: ^2.0.5 
  get_it: ^7.7.0 
  dartz: ^0.10.1 
  
  # --- DATABASE (LOKAL) ---
  drift: ^2.18.0 
  sqlite3_flutter_libs: ^0.5.21 
  path_provider: ^2.1.3 
  path: ^1.9.0 
  shared_preferences: ^2.2.3 

  # --- UI & VISUALISASI ---
  # =================================================================
  # INI DIA PERBAIKANNYA (Versi dinaikin)
  # =================================================================
  fl_chart: ^1.1.1 # <-- Versi baru
  table_calendar: ^3.1.3 # <-- Versi baru
  flutter_colorpicker: ^1.1.0 

  # --- FITUR (HARDWARE & FILE) ---
  image_picker: ^1.1.2 
  image_cropper: ^5.0.1
  file_picker: ^10.3.6 # <-- Versi baru (buat fix Registrar)
  share_plus: ^12.0.1 
  csv: ^6.0.0 # <-- Versi baru
  # =================================================================

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2 # Versi baru

  # --- CODE GENERATOR ---
  build_runner: ^2.4.9 
  drift_dev: ^2.18.0 

  # --- TESTING ---
  bloc_test: ^9.1.7 
  mocktail: ^1.0.3 

flutter:
  uses-material-design: true